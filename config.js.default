window.config = {
    projects: [
        {
            title: 'Some title',
            key: 'Rollbar read access token'
        },
        //...
    ],
    mapping: {
        warning: /staging/,
        danger: /prod/
    },
    refreshInterval: 20 // in seconds
}